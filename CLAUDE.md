# Congranulations

Lua project.

## Build & Run

```bash
lua main.lua          # Run (once main entry point exists)
```

## Code Style

- Lua 5.4 unless otherwise specified
- 2-space indentation
- `snake_case` for variables and functions
- `PascalCase` for modules/classes
- Local by default — avoid globals
